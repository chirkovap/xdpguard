# XDPGuard - High-Performance DDoS Protection System

![License](https://img.shields.io/badge/license-MIT-blue.svg)
![Python](https://img.shields.io/badge/python-3.8%2B-blue.svg)
![Linux](https://img.shields.io/badge/platform-Linux-green.svg)

## 🚀 О проекте

XDPGuard - это высокопроизводительная система защиты от DDoS-атак, использующая технологии XDP (eXpress Data Path) и eBPF для фильтрации трафика на уровне сетевого драйвера. Система обеспечивает производительность до 26 миллионов пакетов в секунду, что в 10-20 раз быстрее традиционных решений на основе iptables.

### ⚡ Ключевые возможности

- **Высокая производительность**: Обработка до 26 Mpps благодаря XDP
- **Минимальная задержка**: Фильтрация на уровне NIC драйвера
- **Веб-панель управления**: Удобный интерфейс на Flask с real-time статистикой
- **CLI инструменты**: Полный набор команд для управления
- **Автоматическая защита**: Rate limiting и IP blacklisting
- **Статистика в реальном времени**: Мониторинг атак и трафика
- **systemd интеграция**: Автоматический запуск сервиса
- **Простая установка**: Скрипт автоматической установки

## 🏗️ Архитектура

```
NIC Driver (eth0) → XDP/eBPF Program (C) → Kernel Space
                           ↓
                    BPF Maps (blacklist)
                           ↓
            Python Control Daemon ← Flask Web Panel
                           ↓
                    REST API & CLI Tools
```

### Компоненты системы

1. **XDP/eBPF Program** (`xdp_filter.c`) - Фильтрация трафика в kernel space
2. **Python Management** (`xdpmanager.py`) - Управление XDP программами через BCC
3. **Web Dashboard** (`web/`) - Flask-based панель управления
4. **CLI Tools** - Интерфейс командной строки
5. **systemd Service** - Автоматизация запуска

## 📦 Требования

### Система
- Linux kernel 4.18+ с поддержкой XDP
- Ubuntu 20.04+, Debian 11+, CentOS 8+, RHEL 8+
- Root права для установки

### Пакеты
- Python 3.8+
- clang, llvm
- libbpf-dev
- python3-bcc
- kernel headers

## 🔧 Установка

### Быстрая установка

```bash
# 1. Клонировать репозиторий
git clone https://github.com/chirkovap/xdpguard.git
cd xdpguard

# 2. Запустить установку (требуется root)
sudo chmod +x scripts/install.sh
sudo ./scripts/install.sh

# 3. Настроить конфигурацию
sudo nano /etc/xdpguard/config.yaml
# Укажите ваш сетевой интерфейс (eth0, ens3, и т.д.)

# 4. Запустить сервис
sudo systemctl start xdpguard
sudo systemctl status xdpguard

# 5. Открыть веб-панель
# http://your-server-ip:8080
```

### Ручная установка

```bash
# Установка зависимостей (Ubuntu/Debian)
sudo apt-get update
sudo apt-get install -y python3-pip clang llvm libelf-dev \
    gcc-multilib linux-headers-$(uname -r) python3-bcc \
    bpfcc-tools libbpf-dev make git

# Установка Python пакетов
pip3 install -r requirements.txt

# Компиляция XDP программ
cd bpf
make
sudo make install
cd ..

# Создание директорий
sudo mkdir -p /etc/xdpguard /var/lib/xdpguard /var/log

# Копирование файлов
sudo cp config.yaml /etc/xdpguard/
sudo cp systemd/xdpguard.service /etc/systemd/system/

# Активация сервиса
sudo systemctl daemon-reload
sudo systemctl enable xdpguard
sudo systemctl start xdpguard
```

## 🎮 Использование

### Веб-интерфейс

Откройте браузер и перейдите по адресу: `http://your-server-ip:8080`

Функции веб-панели:
- Real-time статистика (пакеты, блокировки)
- Блокировка/разблокировка IP адресов
- Просмотр списка заблокированных IP
- Мониторинг производительности
- Графики трафика

### CLI команды

```bash
# Показать статус системы
xdpguard status

# Заблокировать IP адрес
xdpguard block 192.168.1.100

# Разблокировать IP адрес
xdpguard unblock 192.168.1.100

# Показать заблокированные IP
xdpguard list-blocked

# Запустить защиту
xdpguard start

# Остановить защиту
xdpguard stop
```

### Управление сервисом

```bash
# Запустить сервис
sudo systemctl start xdpguard

# Остановить сервис
sudo systemctl stop xdpguard

# Перезапустить сервис
sudo systemctl restart xdpguard

# Статус сервиса
sudo systemctl status xdpguard

# Логи сервиса
sudo journalctl -u xdpguard -f
```

## ⚙️ Конфигурация

Файл конфигурации: `/etc/xdpguard/config.yaml`

```yaml
network:
  interface: eth0          # Сетевой интерфейс для защиты
  mode: router             # Режим: router или bridge

protection:
  enabled: true
  syn_rate: 30             # SYN пакетов в секунду
  syn_burst: 50            # Burst для SYN
  whitelist_ips:
    - 10.0.0.0/8
    - 192.168.0.0/16

web:
  host: 0.0.0.0
  port: 8080
  secret_key: "change-this-secret-key"

logging:
  level: INFO
  file: /var/log/xdpguard.log
```

## 📊 Сравнение производительности

| Решение | Пакетов/сек | Задержка | Нагрузка CPU |
|---------|-------------|----------|---------------|
| **XDPGuard (XDP)** | 26 Mpps | < 10 μs | Низкая |
| iptables | 1-2 Mpps | 50-100 μs | Высокая |
| Traditional firewall | 0.5-1 Mpps | 100+ μs | Очень высокая |

### Преимущества XDP

- **10-20x быстрее** iptables
- Обработка на уровне **NIC драйвера** (до kernel network stack)
- Минимальная **задержка** и нагрузка на CPU
- Эффективна против **volumetric атак** (100+ Gbps)

## 🔒 Безопасность

- Сервис работает с правами root (требуется для XDP)
- Веб-панель использует secret key для защиты сессий
- Рекомендуется использовать firewall для ограничения доступа к порту 8080
- IP whitelist для защищённых адресов

## 🧪 Тестирование

```bash
# Проверка загрузки XDP программы
sudo bpftool prog show
sudo ip link show dev eth0

# Тест блокировки IP
curl -X POST http://localhost:8080/api/block \
  -H "Content-Type: application/json" \
  -d '{"ip": "192.168.1.100"}'

# Проверка статистики
curl http://localhost:8080/api/status
```

## 📁 Структура проекта

```
xdpguard/
├── bpf/
│   ├── xdp_filter.c         # XDP программа на C
│   └── Makefile             # Компиляция eBPF
├── python/
│   ├── xdpmanager.py        # Управление XDP через BCC
│   └── config.py            # Конфигурация
├── web/
│   ├── app.py               # Flask приложение
│   └── templates/
│       └── dashboard.html   # Веб-интерфейс
├── scripts/
│   └── install.sh           # Установочный скрипт
├── systemd/
│   └── xdpguard.service     # systemd unit
├── config.yaml              # Конфигурация по умолчанию
├── requirements.txt         # Python зависимости
├── daemon.py                # Главный демон
└── README.md
```

## 🛠️ Разработка

### Модификация XDP программы

```bash
cd bpf
nano xdp_filter.c  # Редактировать C код
make               # Компилировать
sudo make install  # Установить
```

### Отладка

```bash
# Логи системы
sudo journalctl -u xdpguard -f

# Логи приложения
sudo tail -f /var/log/xdpguard.log

# BPF trace
sudo bpftool prog tracelog
```

## 📚 Технологии

- **XDP (eXpress Data Path)**: In-kernel fast path для обработки пакетов
- **eBPF**: Безопасное выполнение кода в kernel space
- **BCC (BPF Compiler Collection)**: Python интерфейс для eBPF
- **Flask**: Веб-фреймворк для панели управления
- **Bootstrap 5**: UI фреймворк
- **Chart.js**: Графики и визуализация

## 🌟 Вдохновение

Проект вдохновлён enterprise-решениями:
- Cloudflare XDP DDoS Protection
- Facebook Katran L4 Load Balancer
- Cilium Network Security

## 📝 Дипломная работа

Этот проект разработан как часть дипломной работы на тему:
**"Анализ и построение системы фильтрации DDoS-трафика на Linux"**

Включает:
- Прототип на основе iptables
- Продвинутая реализация XDP/eBPF
- Сравнительный анализ производительности
- Enterprise-уровень функциональности

## 🤝 Вклад в проект

Приветствуются pull requests! Для значительных изменений сначала откройте issue для обсуждения.

## 📄 Лицензия

MIT License - см. файл [LICENSE](LICENSE)

## 👤 Автор

**chirkovap**
- GitHub: [@chirkovap](https://github.com/chirkovap)

## 🙏 Благодарности

- Linux kernel XDP/eBPF community
- BCC project maintainers
- Open-source DDoS protection projects

## 📧 Контакты

Вопросы и предложения - создавайте [GitHub Issues](https://github.com/chirkovap/xdpguard/issues)

---

⭐ Если проект был полезен, поставьте звезду на GitHub!
