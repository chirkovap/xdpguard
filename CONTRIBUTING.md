# Contributing to XDPGuard

Спасибо за интерес к проекту XDPGuard! Мы приветствуем любые вклады в развитие проекта.

## Как внести вклад

### Сообщения об ошибках

Если вы нашли ошибку:

1. Проверьте, не была ли она уже сообщена в [Issues](https://github.com/chirkovap/xdpguard/issues)
2. Создайте новый issue с подробным описанием:
   - Версия ОС и ядра Linux
   - Шаги для воспроизведения
   - Ожидаемое поведение
   - Фактическое поведение
   - Логи ошибок

### Предложения новых функций

1. Откройте issue с тегом `enhancement`
2. Подробно опишите предлагаемую функцию
3. Объясните, почему она будет полезна
4. Приветствуются примеры использования

### Pull Requests

1. **Fork** репозитория
2. Создайте **отдельную ветку** для ваших изменений:
   ```bash
   git checkout -b feature/your-feature-name
   ```
3. Внесите изменения и **протестируйте** их
4. Убедитесь, что код соответствует стилю проекта
5. Создайте **коммиты** с понятными сообщениями:
   ```bash
   git commit -m "Add feature: description"
   ```
6. **Push** в ваш fork:
   ```bash
   git push origin feature/your-feature-name
   ```
7. Создайте **Pull Request** в основной репозиторий

## Стандарты кода

### Python

- Следуйте [PEP 8](https://www.python.org/dev/peps/pep-0008/)
- Используйте docstrings для функций и классов
- Добавляйте type hints где возможно
- Код должен работать на Python 3.8+

```python
def function_name(param: str) -> bool:
    """Short description.
    
    Args:
        param: Parameter description
        
    Returns:
        Description of return value
    """
    pass
```

### C (eBPF)

- Следуйте стилю Linux kernel
- Используйте осмысленные имена переменных
- Добавляйте комментарии для сложной логики
- Проверяйте границы буферов

### Структура коммитов

```
Тип: Краткое описание (до 50 символов)

Подробное описание изменений (если необходимо).
Может быть несколько параграфов.

- Пункт списка изменений
- Ещё один пункт
```

**Типы коммитов:**
- `feat`: Новая функция
- `fix`: Исправление ошибки
- `docs`: Изменения в документации
- `style`: Форматирование, без изменения логики
- `refactor`: Рефакторинг кода
- `test`: Добавление тестов
- `chore`: Обновление зависимостей, конфигурации и т.д.

## Тестирование

Перед отправкой PR:

1. Убедитесь, что XDP программа компилируется:
   ```bash
   cd bpf && make
   ```

2. Проверьте Python код:
   ```bash
   python3 -m py_compile python/*.py
   python3 -m py_compile web/*.py
   ```

3. Протестируйте установку:
   ```bash
   sudo ./scripts/install.sh
   ```

4. Проверьте работу сервиса:
   ```bash
   sudo systemctl start xdpguard
   sudo systemctl status xdpguard
   ```

## Области для вклада

### Приоритетные задачи

- [ ] Добавление unit-тестов
- [ ] Улучшение алгоритмов обнаружения DDoS
- [ ] Поддержка IPv6
- [ ] Интеграция с Prometheus/Grafana
- [ ] Telegram/Email уведомления
- [ ] Автоматическая разблокировка IP после таймаута
- [ ] Geo-IP фильтрация
- [ ] Более детальная статистика по типам атак

### Документация

- Перевод на английский язык
- Руководства по развёртыванию
- Примеры конфигураций
- Видео-туториалы
- Сравнение с аналогами

### Инфраструктура

- CI/CD pipeline
- Docker образы
- Ansible playbooks
- Terraform модули
- Helm charts для Kubernetes

## Лицензия

Отправляя PR, вы соглашаетесь с тем, что ваш код будет распространяться под лицензией MIT.

## Вопросы?

Если у вас есть вопросы, создайте [issue](https://github.com/chirkovap/xdpguard/issues) или свяжитесь с мейнтейнером.

## Благодарности

Спасибо всем, кто вносит вклад в развитие XDPGuard! 🎉
